TEAM_ID=

PRODUCT_NAME=Æther
PRODUCT_BUNDLE_IDENTIFIER=com.oeuvre.aether.Aether$(TEAM_ID)

CURRENT_PROJECT_VERSION=1
MARKETING_VERSION=1.0

// Paste your Gemini API key here. Never commit a real key — use a local override file instead.
GEMINI_API_KEY=VALUE

// Paste your Google Places API key here. Never commit a real key — use a local override file instead.
PLACES_API_KEY=VALUE